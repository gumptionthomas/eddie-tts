# Core API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Audio processing
soundfile>=0.12.0
numpy>=1.24.0

# Qwen3-TTS (installed separately with specific torch version)
# pip install -U qwen-tts

# Optional: Flash Attention 2 (recommended for reduced memory)
# pip install -U flash-attn --no-build-isolation
